; Only resolve dependency versions that have been on the registry for a week,
; so a compromised release has time to be found and pulled before we take it.
; Requires npm 11.10 or later; older versions ignore it.
; It does not affect `npm ci`, which installs exactly what the lockfile pins.
min-release-age=7
